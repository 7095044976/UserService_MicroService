server:
  port: 8081
Spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/userdb_dev
    username: devuser
    password: devpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true